name: Build and Test

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    name: Build on ${{ matrix.os }} with Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ['3.9', '3.10', '3.11', '3.12']

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Full history for setuptools-scm
        lfs: false  # Don't auto-pull LFS, we'll cache it

    - name: Cache Git LFS files
      uses: actions/cache@v4
      id: lfs-cache
      with:
        path: .git/lfs
        key: lfs-${{ hashFiles('.gitattributes') }}

    - name: Download essential test data from Zenodo
      run: |
        # Download Sentinel-2 LUT from Zenodo (avoids LFS quota issues)
        mkdir -p tests/data
        echo "Downloading Sentinel-2 LUT from Zenodo (70 MB)..."
        curl -L -o tests/data/lut_sentinel2b_b2to12_3um_dust.mat \
          https://zenodo.org/records/18701286/files/lut_sentinel2b_b2to12_3um_dust.mat
        echo "✓ Essential test data downloaded"

    - name: Download large test data (optional)
      run: |
        # Optionally download full test data from Zenodo
        # Uncomment to enable full-resolution tests (adds ~15 minutes to CI time)
        # curl -L -o tests/data/LUT_MODIS.mat https://zenodo.org/records/18701286/files/LUT_MODIS.mat
        # curl -L -o tests/data/sentinel_r.nc https://zenodo.org/records/18704072/files/sentinel_r.nc
        # curl -L -o tests/data/sentinel_r0.nc https://zenodo.org/records/18704072/files/sentinel_r0.nc
        echo "Using essential test data only"
        echo "Full datasets available at:"
        echo "  LUTs: https://doi.org/10.5281/zenodo.18701286"
        echo "  Imagery: https://doi.org/10.5281/zenodo.18704072"

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}

    - name: Setup Miniconda
      uses: conda-incubator/setup-miniconda@v3
      with:
        auto-update-conda: true
        python-version: ${{ matrix.python-version }}
        channels: conda-forge
        channel-priority: strict

    - name: Install build dependencies from conda-forge
      shell: bash -l {0}
      run: |
        conda install -c conda-forge swig gcc gxx nlopt

    - name: Install Python dependencies
      shell: bash -l {0}
      run: |
        python -m pip install --upgrade pip
        pip install setuptools-scm wheel
        pip install '.[test]'

    - name: Build SWIG extensions
      shell: bash -l {0}
      run: |
        python setup.py build_ext --inplace

    - name: Run tests
      shell: bash -l {0}
      run: |
        # Skip tests requiring LUT_MODIS.mat (legacy MODIS tests)
        if [ ! -f tests/data/LUT_MODIS.mat ]; then
          echo "Skipping MODIS-specific tests (LUT_MODIS.mat not available)"
          pytest -v --ignore=tests/test_legacy.py
        else
          pytest -v
        fi

    - name: Test import
      shell: bash -l {0}
      run: |
        python -c "import spires; print(f'SpiPy version: {spires.__version__}')"
